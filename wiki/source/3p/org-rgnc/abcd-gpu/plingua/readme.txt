
This is a redistributable stand-alone version of pLinguaCore that includes a compiler for generating binary files from PDP systems models in P-Lingua.

The syntax of the application to be executed in command line (or terminal) is:

> java -jar pLinguaCore.jar plingua YOUR_INPUT_PLINGUA_FILE.pli -bin YOUR_OUTPUT_BINARY_FILE.bin


